# =============================================================================
# Benchmark Configuration for black-forest-labs/FLUX.1-schnell
# =============================================================================

# === Test Cases (Workloads) ===
cases:
  # Text-to-Image test case
  - id: "flux_t2i_test"
    prompt: "A futuristic cyberpunk city at night, neon lights reflecting on wet streets, highly detailed, 8k"
    task: "image"
    params:
    num_inference_steps: 30
    height: 1024
    width: 1024
    seed: 42

  # Image-to-Image/Image Editing test case
  - id: "image_edit_test"
    prompt: "Transform into anime style"
    task: "image"
    params:
    num_inference_steps: 30
    height: 1024
    width: 1024
    image_path: "https://huggingface.co/datasets/huggingface/documentation-images/resolve/main/diffusers/cat.png"
    seed: 42

# === Evaluation Targets ===
targets:
  # Diffusers - Flux (Image Generation)
  - name: "diffusers_flux"
    engine_class: "DiffusersEngine"
    type: "local"
    task: "image"
    output_dir: "outputs"
    params:
      model: "black-forest-labs/FLUX.1-schnell"

  # # SGLang - Flux (Image Generation)
  # - name: "sglang_flux_schnell"
  #   engine_class: "SGLangEngine"
  #   type: "local"
  #   task: "image"
  #   model: "black-forest-labs/FLUX.1-schnell"
  #   output_dir: "outputs"
